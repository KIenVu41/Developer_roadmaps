// string methods = allow you to manipulate and work with text (strings)

let userName = "Kevin";

userName.charAt(0);
userName.indexOf("e");
userName.length;
userName.trim();
userName.toUpperCase();
userName.toLowerCase();
userName.repeat();
userName.startsWith("Ke");
userName.endsWith(" ");
userName.includes(" ");
userName.replaceAll("-", "");
userName.padStart(3, "0");
userName.padEnd(1, "0");